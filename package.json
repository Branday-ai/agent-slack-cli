{
  "name": "agent-slack-cli",
  "version": "1.0.0",
  "description": "Slack CLI for AI agent projects. Post, search, check, upload, react — all from the command line.",
  "type": "module",
  "bin": {
    "slack": "./bin/slack.sh"
  },
  "files": [
    "bin/",
    "src/"
  ],
  "dependencies": {
    "@slack/web-api": "^7.9.0",
    "dotenv": "^16.4.0"
  },
  "peerDependencies": {
    "tsx": "^4.19.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
